import React, { Component } from 'react';

/**
 * Representational component for Not found. User will be redirected to this page, if an API call fails.
 */
class NotFound extends Component {
    render() {
        return (
            <div>
                 NotFound
            </div>
        )
    }
}
export default NotFound;
export function getFLashcardSets(payload) {
    //TODO: service call to get flashcards sets
    return{
        type: 'GET_FLASHCARD_SETS',
        data: payload //will change to response
    }
}

export function getExamList(payload) {
    //TODO: service call to get exam list
    return{
        type: 'GET_EXAM_LIST',
        data: payload // will change to reponse
    }
}

export function getDiscussionList(payload) {
    //TODO: service call to get discussion list
    return{
        type: 'GET_DISCUSSION_LIST',
        data: payload //will change to response
    }
}

export function getGrades(payload) {
    //TODO: service call to get grades
    return{
        type: 'GET_GRADES',
        data: payload //will change to response
    }
}

export function getFiles(payload) {
    //TODO: service call to get Files
    return{
        type: 'GET_FILES',
        data: payload //will change to response
    }
}

